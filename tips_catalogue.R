#1: to set the working directory, do this:
setwd("/the/path/to/the/directory")


#2: fivenum() is a convenient way to display the min, lower quartile, mean, upper quartile, and max of a set.
#3: vioplot() for "violin plot" is a more visually appealing alternative to the traditional boxplot().  See package "vioplot".
#4: stackpoly() is more effective than polygon() for filling in sections of plots.
